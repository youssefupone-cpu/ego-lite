# star-history (generated branch)

Machine-generated. Do not edit, do not open pull requests against this branch,
and do not expect its history to be stable — every run of
[`.github/workflows/star-history.yml`](https://github.com/citrolabs/ego-lite/blob/main/.github/workflows/star-history.yml)
on `main` force-pushes a fresh single commit over it.

The chart lives here rather than on `main` because `main`'s ruleset requires a
reviewed pull request for every change, which a scheduled bot cannot produce
unattended. `main`'s README links these files by raw URL:

| File | Purpose |
| --- | --- |
| `star-history-light.svg` | README chart, light theme |
| `star-history-dark.svg` | README chart, dark theme |
| `star-history.png` | Raster fallback for registries that cannot render SVG |
| `.star-history.sig` | Signature over the star data; how the workflow detects a change |
